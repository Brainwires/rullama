# rullama-cli — the agentic CLI (brand: rullama). Extracted from
# brainwires-framework into its own repo. Standalone workspace: brainwires-*
# deps resolve via path to a sibling ../brainwires-framework checkout (flip to
# crates.io version pins when the harness is published).
[workspace]
resolver = "2"

[workspace.package]
version = "0.11.0"
edition = "2024"
rust-version = "1.91"
license = "MIT"
repository = "https://github.com/Brainwires/rullama-cli"
homepage = "https://github.com/Brainwires/rullama-cli"
authors = ["Brainwires"]

[package]
name = "rullama-cli"
version.workspace = true
edition.workspace = true
description = "An AI-powered agentic CLI tool for autonomous coding assistance"
license = "MIT"
authors = ["Brainwires Team"]
keywords = ["ai", "agent", "cli", "coding-assistant", "autonomous", "llm"]
repository = "https://github.com/Brainwires/rullama-cli"
homepage = "https://github.com/Brainwires/rullama-cli"
publish = false

[[bin]]
name = "rullama"
path = "src/main.rs"
doc = false

[dependencies]
# Async runtime
tokio.workspace = true
tokio-util.workspace = true
futures.workspace = true
async-trait.workspace = true
async-stream.workspace = true

# CLI framework
clap = { version = "4", features = ["derive", "env", "color"] }
dialoguer = "0.11"
indicatif = "0.17"
console = "0.15"

# TUI framework
ratatui = { version = "0.30", features = ["unstable-rendered-line-info"] }
crossterm = { version = "0.29", features = ["event-stream"] }
termimad = "0.34"

# TUI extension components (checkbox, button, input, dialogs)
# Uses local dev version — published 0.4.2 lacks CursorMode, ScrollMode, delete_word_forward, title()
ratatui-interact = { version = "0.5.2", features = ["clipboard"] }

# HTTP client
reqwest.workspace = true

# OpenSSL vendoring for cross-compilation
openssl = { version = "0.10", features = ["vendored"] }

# WebSocket
tokio-tungstenite.workspace = true

# Supabase
postgrest = "1.6"

# MCP Protocol
rmcp.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true
serde_yml.workspace = true
toml.workspace = true

# File operations
walkdir.workspace = true
glob.workspace = true
ignore.workspace = true
diffy.workspace = true

# Git
git2.workspace = true

# Clipboard
arboard = "3.4"

# Compression
zstd.workspace = true
flate2.workspace = true

# Utilities
anyhow.workspace = true
thiserror.workspace = true
regex.workspace = true
chrono.workspace = true
dirs.workspace = true
lazy_static = "1.5"
lru.workspace = true
sha2.workspace = true
base64.workspace = true
uuid.workspace = true
gethostname.workspace = true

# Password/crypto
bcrypt = "0.15"
hex.workspace = true
subtle.workspace = true
rand.workspace = true
keyring.workspace = true
zeroize.workspace = true
chacha20poly1305.workspace = true

# SQLite
rusqlite.workspace = true

# Logging/tracing
tracing.workspace = true
tracing-subscriber = { workspace = true, features = ["env-filter", "json"] }
tracing-appender = "0.2"

# Vector database
lancedb.workspace = true
arrow.workspace = true
arrow-array.workspace = true
arrow-schema.workspace = true

# Performance
rayon = "1.10"

# Glob pattern matching
globset = "0.4"

# Document processing
zip = "2.2"

# Schema validation
schemars.workspace = true

# URL parsing
url.workspace = true

# HTTP types
http.workspace = true
hyper.workspace = true
hyper-util.workspace = true
http-body-util.workspace = true
bytes.workspace = true

# Web scraping
scraper = "0.24"
parking_lot = "0.12"
urlencoding.workspace = true

# Adaptive prompting
ndarray.workspace = true
linfa.workspace = true
linfa-clustering.workspace = true
bincode.workspace = true

# Rhai scripting engine
rhai = { version = "1.23", features = ["sync", "serde"] }

# ── Brainwires Framework facade ──
brainwires = { path = "../brainwires-framework/crates/brainwires", features = ["full"] }

# ── Brainwires Framework crates (custom feature flags) ──
brainwires-agent = { workspace = true }
brainwires-mdap = { workspace = true }
brainwires-skills = { workspace = true, features = ["registry"] }
brainwires-seal = { workspace = true }
brainwires-eval = { workspace = true }
brainwires-inference = { workspace = true }
brainwires-tool-runtime = { workspace = true, features = ["native", "orchestrator", "rag"] }
brainwires-tool-builtins = { workspace = true, features = ["native", "interpreters", "rag"] }
brainwires-knowledge = { workspace = true, features = ["alt-folder-name"] }
brainwires-rag = { workspace = true, features = ["documents"] }
brainwires-network = { workspace = true, features = ["auth-keyring"] }
brainwires-provider.workspace = true
brainwires-telemetry = { workspace = true, features = ["native"] }
brainwires-storage = { workspace = true, features = ["native"] }
brainwires-stores = { workspace = true, features = ["session", "task", "plan", "conversation", "memory", "lock", "image"] }
brainwires-memory = { workspace = true }
brainwires-core = { workspace = true, features = ["native"] }

petgraph = "0.8.3"
nix = { version = "0.30.1", features = ["term", "signal", "process"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = ["Win32_Foundation", "Win32_System_Threading"] }

[dev-dependencies]
tempfile.workspace = true
tokio-test = "0.4"
mockito = "1.5"
assert_cmd = "2.0"
predicates = "3.1"
dotenvy = "0.15"
proptest = "1.5"

[features]
default = []
interpreter-javascript = []
interpreter-python = []
interpreter-all = ["interpreter-javascript", "interpreter-python"]
piston = []
bedrock = ["brainwires-provider/bedrock"]
vertex-ai = ["brainwires-provider/vertex-ai"]
llama-cpp-2 = ["brainwires-provider/llama-cpp-2"]
full = ["interpreter-all", "llama-cpp-2", "bedrock", "vertex-ai"]

[build-dependencies]
chrono = "0.4"

[workspace.dependencies]
# Internal crates
brainwires-core = { path = "../brainwires-framework/crates/brainwires-core", version = "0.11.0", default-features = false }
brainwires-tool-runtime = { path = "../brainwires-framework/crates/brainwires-tool-runtime", version = "0.11.0", default-features = false }
brainwires-tool-builtins = { path = "../brainwires-framework/crates/brainwires-tool-builtins", version = "0.11.0", default-features = false }
brainwires-agent = { path = "../brainwires-framework/crates/brainwires-agent", version = "0.11.0", default-features = false }
brainwires-storage = { path = "../brainwires-framework/crates/brainwires-storage", version = "0.11.0", default-features = false }
brainwires-stores = { path = "../brainwires-framework/crates/brainwires-stores", version = "0.11.0", default-features = false }
brainwires-memory = { path = "../brainwires-framework/crates/brainwires-memory", version = "0.11.0" }
brainwires-mdap = { path = "../brainwires-framework/crates/brainwires-mdap", version = "0.11.0" }
brainwires-skills = { path = "../brainwires-framework/crates/brainwires-skills", version = "0.11.0" }
brainwires-seal = { path = "../brainwires-framework/crates/brainwires-seal", version = "0.11.0", default-features = false }
brainwires-eval = { path = "../brainwires-framework/crates/brainwires-eval", version = "0.11.0" }
brainwires-inference = { path = "../brainwires-framework/crates/brainwires-inference", version = "0.11.0", default-features = false }
brainwires-mcp-client = { path = "../brainwires-framework/crates/brainwires-mcp-client", version = "0.11.0" }
brainwires-permission = { path = "../brainwires-framework/crates/brainwires-permission", version = "0.11.0" }
brainwires-sandbox = { path = "../brainwires-framework/crates/brainwires-sandbox", version = "0.11.0", default-features = false }
brainwires-network = { path = "../brainwires-framework/crates/brainwires-network", version = "0.11.0" }
brainwires-mcp-server = { path = "../brainwires-framework/crates/brainwires-mcp-server", version = "0.11.0" }
brainwires-a2a = { path = "../brainwires-framework/crates/brainwires-a2a", version = "0.11.0", default-features = false }
brainwires-provider = { path = "../brainwires-framework/crates/brainwires-provider", version = "0.11.0", default-features = false, features = ["native"] }
brainwires-provider-speech = { path = "../brainwires-framework/crates/brainwires-provider-speech", version = "0.11.0", default-features = false }
brainwires-call-policy = { path = "../brainwires-framework/crates/brainwires-call-policy", version = "0.11.0", default-features = false }
brainwires-hardware = { path = "../brainwires-framework/crates/brainwires-hardware", version = "0.11.0" }
brainwires-finetune = { path = "../brainwires-framework/crates/brainwires-finetune", version = "0.11.0" }
brainwires-datasets = { path = "../brainwires-framework/crates/brainwires-datasets", version = "0.11.0", default-features = false }
# `brainwires-finetune-local` and `brainwires-training` were moved out of this
# framework into the `rullama` workspace (LoRA/QLoRA/DoRA + future
# training-from-scratch live there now). See CHANGELOG [Unreleased].
brainwires-knowledge = { path = "../brainwires-framework/crates/brainwires-knowledge", version = "0.11.0", default-features = false }
brainwires-rag = { path = "../brainwires-framework/crates/brainwires-rag", version = "0.11.0", default-features = false }
brainwires-prompting = { path = "../brainwires-framework/crates/brainwires-prompting", version = "0.11.0", default-features = false }
brainwires-reasoning = { path = "../brainwires-framework/crates/brainwires-reasoning", version = "0.11.0" }
brainwires-telemetry  = { path = "../brainwires-framework/crates/brainwires-telemetry",  version = "0.11.0", default-features = false }
# `brainwires-autonomy` and `brainwires-proxy` live in `extras/`. They are
# workspace-only (publish = false) and no `crates/` member depends on them, so
# they intentionally do NOT appear in [workspace.dependencies] — that table is
# the API surface of the framework. See ADR-0004 (framework / extras boundary)
# and `cargo xtask lint-deps`.

# Tokenizer dependencies
tokenizers = "0.22"
tiktoken-rs = "0.9"

# `burn-*` was used by the deleted `brainwires-finetune-local`; the local
# PEFT crate now lives in the `rullama` workspace, where Burn is pinned
# directly. No framework crate depends on Burn anymore.

# Home automation serial protocols
tokio-serial = "5.4"
# CRC calculations (Zigbee EZSP/ASH CRC-16-CCITT)
crc = "3"
# mDNS service discovery (Matter device/controller discovery)
mdns-sd = "0.12"

# Async runtime & utilities
tokio = { version = "1.43" }
futures = "0.3"
async-trait = "0.1"
async-stream = "0.3"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yml = "0.0.12"

# Error handling
anyhow = "1"
thiserror = "2"

# Identifiers & time
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

# HTTP
reqwest = { version = "0.12", features = ["json", "stream"] }
axum = "0.8"

# MCP protocol
rmcp = { version = "1.1", features = ["client", "server", "client-side-sse", "transport-child-process", "transport-io", "macros"] }

# MCP server support
schemars = "1.0"
tokio-util = "0.7"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Vector database & columnar storage
lancedb = "0.27"
arrow = "57"
arrow-array = "57"
arrow-schema = "57"

# Embeddings
fastembed = "5.1"

# Caching
lru = "0.16"

# Semantic versioning
semver = { version = "1", features = ["serde"] }

# Hashing
sha2 = "0.10"

# SQLite
rusqlite = { version = "0.38", features = ["bundled"] }

# File operations
walkdir = "2.5"
diffy = "0.4"
glob = "0.3"

# Code search
regex = "1"
ignore = "0.4"

# URL encoding
urlencoding = "2"

# Filesystem paths
dirs = "6"

# Git
git2 = "0.20"

# Security
zeroize = { version = "1", features = ["derive"] }

# Config parsing
toml = "1.0"

# ML / Clustering
linfa = "0.7"
linfa-clustering = "0.7"
ndarray = "0.15"

# Bitflags
bitflags = "2"

# Binary encoding
bincode = { version = "2", features = ["serde"] }

# Crypto
chacha20poly1305 = "0.10"
rand = "0.10"
rand_core = { version = "0.6", features = ["getrandom"] }
base64 = "0.22"
hex = "0.4"
subtle = "2.6"
p256    = { version = "0.13.2", features = ["ecdh", "ecdsa", "arithmetic"] }
ecdsa   = { version = "0.16.9", features = ["der", "signing", "verifying"] }
hmac    = "0.12.1"
hkdf    = "0.12.4"
pbkdf2  = { version = "0.12.2", features = ["hmac"] }
aes     = "0.8.4"
ccm     = { version = "0.5.0", features = ["std"] }
der     = { version = "0.8.0", features = ["alloc", "derive"] }
pkcs8   = { version = "0.10.2", features = ["alloc"] }

# Keyring (secure key storage)
keyring = "3"

# Concurrent maps
dashmap = "6"

# WebSocket
tokio-tungstenite = { version = "0.26", features = ["native-tls"] }

# Discord API
serenity = { version = "0.12", default-features = false, features = ["client", "gateway", "model", "cache", "rustls_backend", "chrono"] }

# Telegram Bot API
teloxide = { version = "0.13", features = ["macros"] }

# URL parsing
url = "2.5"

# Hostname
gethostname = "1.0"

# Compression
flate2 = "1.0"
zstd = "0.13"

# Local LLM inference (optional)
llama-cpp-2 = "0.1"

# AWS SDK (Bedrock auth)
aws-sigv4 = "1"
aws-credential-types = "1"
aws-smithy-runtime-api = "1"

# GCP auth (Vertex AI)
gcp_auth = "0.12"

# HTTP (hyper ecosystem)
hyper = { version = "1", features = ["server", "client", "http1", "http2"] }
hyper-util = { version = "0.1", features = ["tokio", "server-auto", "client-legacy"] }
http = "1.1"
http-body = "1"
http-body-util = "0.1"
bytes = "1"

# gRPC
tonic = "0.13"
prost = "0.14"
prost-types = "0.14"

# Neo4j Bolt protocol
neo4rs = "0.9.0-rc.9"

# Token decoding (used by llama-cpp-2)
encoding_rs = "0.8"

# Async stream adapters
tokio-stream = "0.1"

# Postgres + pgvector (using tokio-postgres to avoid sqlx's libsqlite3-sys conflict)
tokio-postgres = "0.7"
deadpool-postgres = "0.14"
pgvector = { version = "0.4.1", features = ["postgres"] }

# Testing
tempfile = "3"
proptest = "1"

# ── Build profiles ──────────────────────────────────────────────────────────
#
# `dev` and `test` profiles emit minimal debug info (file:line only). Full
# debug info on the knowledge/storage test binaries pulls in lancedb,
# datafusion, arrow, tantivy, tree-sitter-*, and image processing, and the
# linker OOMs on stock GitHub Actions runners (signal 7, ld bus error).
# Line tables keep panic stack traces readable without the binary-size
# explosion.
#
# Release builds keep the default (full debug). Override per-crate if a
# specific consumer needs richer debug info.
