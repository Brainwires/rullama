{
  "name": "rullama-cloud-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker BYOK proxy for rullama cloud chat (Ollama Cloud + OpenAI).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
